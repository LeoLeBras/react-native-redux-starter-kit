/* @flow */

import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    flexDirection: 'row',
  },

  value: {
    width: 40,
    textAlign: 'center',
  },
  
  image: {
    width: 50,
    height: 50
  }
  
})
