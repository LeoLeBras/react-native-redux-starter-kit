/* @flow */

import React from 'react'
import LaunchContainer from '@containers/LaunchContainer'

const LaunchScene = (props: any): React$Element => {
  return (
    <LaunchContainer {...props} />
  )
}

export default LaunchScene;
